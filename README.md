# TelegramBot.Webhook

Production-ready Telegram bot для поиска информации о российских компаниях, аналог @egrul_bot.

## Архитектура

- **Telegram Bot API** - пользовательский интерфейс
- **OpenAI Assistant API** - логика и форматирование ответов
- **OpenAI Vector Store** - векторная память для контекста и истории
- **MCP DaData** - единственный источник фактических данных о компаниях
- **Vercel** - serverless хостинг с webhook

## Стек технологий

- Python 3.11
- python-telegram-bot 20.7
- OpenAI API (Assistant + Vector Store)
- MCP DaData (https://mcp.dadata.ru/mcp)
- Vercel Serverless Functions
- ReportLab (PDF export)

## Функционал

### Основные экраны
- 📊 Краткий отчёт
- 💰 Финансы
- 📋 Реквизиты
- 📍 Адрес

### История изменений
- 👤 Директора
- 👥 Учредители
- 📍 История адресов
- 📊 ОКВЭД (виды деятельности)

### Внешние модули
- ⚖️ Судебные дела (sudrf.ru) - best-effort парсинг
- 🏛 Госзакупки (zakupki.gov.ru) - best-effort парсинг

### Дополнительно
- 📄 Экспорт в PDF (текущий экран или полный отчёт)
- iOS-style интерфейс с inline-кнопками
- Постраничный вывод для длинных списков
- Векторная память для контекста и повторных запросов

## Установка и настройка

### 1. Клонирование репозитория

```bash
git clone https://github.com/ArtemFilin1990/TelegramBot.Webhook.git
cd TelegramBot.Webhook
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните значения:

```bash
cp .env.example .env
```

Необходимые переменные:
```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_WEBHOOK_URL=https://your-app.vercel.app/api/webhook

OPENAI_API_KEY=your_openai_api_key_here
OPENAI_ASSISTANT_ID=  # Будет создан автоматически
OPENAI_VECTOR_STORE_ID=  # Будет создан автоматически

DADATA_API_KEY=your_dadata_api_key_here
DADATA_SECRET_KEY=your_dadata_secret_key_here
```

### 4. Создание OpenAI Assistant и Vector Store

```bash
python setup.py create-assistant
```

Скрипт создаст Assistant и Vector Store, выведет их ID для добавления в `.env`.

### 5. Деплой на Vercel

#### Установка Vercel CLI

```bash
npm i -g vercel
```

#### Деплой

```bash
vercel
```

При первом деплое Vercel попросит:
- Логин (если не залогинены)
- Подтверждение настроек проекта
- Название проекта

#### Настройка переменных окружения в Vercel

```bash
vercel env add TELEGRAM_BOT_TOKEN
vercel env add OPENAI_API_KEY
vercel env add OPENAI_ASSISTANT_ID
vercel env add OPENAI_VECTOR_STORE_ID
vercel env add DADATA_API_KEY
vercel env add DADATA_SECRET_KEY
```

### 6. Настройка Telegram Webhook

После деплоя получите URL вашего приложения и настройте webhook:

```bash
python setup.py set-webhook https://your-app.vercel.app/api/webhook
```

Или выполните полную настройку одной командой:

```bash
python setup.py full-setup https://your-app.vercel.app/api/webhook
```

### 7. Проверка статуса

```bash
python setup.py webhook-info
```

## Использование

### Запуск бота

После деплоя на Vercel бот автоматически обрабатывает webhook запросы.

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Справка по использованию
- `/cancel` - Отменить текущую операцию

### Поиск компании

1. Нажмите "🔍 Поиск по ИНН" или "🏢 Поиск по ОГРН"
2. Введите ИНН (10/12 цифр) или ОГРН (13/15 цифр)
3. Получите краткий отчёт с меню действий

### Навигация

Используйте inline-кнопки для навигации по экранам:
- Просмотр различных разделов информации
- Экспорт данных в PDF
- Поиск судебных дел и госзакупок

## Архитектурные принципы

### Векторная память (Vector Store)

Бот сохраняет в Vector Store:
- Историю запросов пользователя
- Сформированные отчёты
- Контекст компании (по ИНН/ОГРН)
- Последние открытые экраны

При повторных запросах используется retrieval перед новым обращением к MCP.

### MCP DaData (STRICT)

**КРИТИЧЕСКИ ВАЖНО:**
- Все данные о компаниях получаются ИСКЛЮЧИТЕЛЬНО через MCP DaData
- Запрещено использовать знания модели или додумывать данные
- Если поле отсутствует - выводится "нет данных"

### OpenAI Assistant

Assistant управляет:
- Логикой обработки запросов
- Форматированием ответов в iOS-стиле
- Работой с векторной памятью
- Генерацией структурированных отчётов

### Ограничения

- Никакого скоринга, рейтингов, оценок "надёжности"
- Только факты и структурированные данные
- Честное информирование об ограничениях внешних источников

## Структура проекта

```
.
├── api/
│   └── webhook.py          # Vercel serverless function
├── bot/
│   ├── handlers/
│   │   ├── main.py         # Основные команды
│   │   ├── search.py       # Поиск по ИНН/ОГРН
│   │   ├── company.py      # Экраны компании
│   │   ├── export.py       # Экспорт в PDF
│   │   └── external.py     # Суды и госзакупки
│   ├── services/
│   │   ├── assistant.py    # OpenAI Assistant
│   │   ├── mcp_dadata.py   # MCP DaData integration
│   │   ├── court.py        # Парсинг sudrf.ru
│   │   ├── procurement.py  # Парсинг zakupki.gov.ru
│   │   └── pdf_export.py   # PDF генерация
│   └── utils/
│       ├── keyboards.py    # Telegram клавиатуры
│       └── formatters.py   # Форматирование сообщений
├── config.py               # Конфигурация
├── setup.py                # Скрипт настройки
├── requirements.txt        # Python зависимости
├── vercel.json            # Конфигурация Vercel
├── .env.example           # Пример переменных окружения
└── README.md              # Документация
```

## Получение API ключей

### Telegram Bot Token
1. Найдите @BotFather в Telegram
2. Создайте нового бота командой `/newbot`
3. Скопируйте полученный токен

### OpenAI API Key
1. Зарегистрируйтесь на https://platform.openai.com
2. Перейдите в API Keys
3. Создайте новый ключ

### DaData API Keys
1. Зарегистрируйтесь на https://dadata.ru
2. Получите API ключ и Secret ключ в личном кабинете
3. Рекомендуется платный тариф для доступа ко всем данным

## Поддержка и разработка

### Логирование

Логи доступны в Vercel Dashboard:
1. Откройте проект в Vercel
2. Перейдите в раздел "Logs"
3. Фильтруйте по функции `api/webhook`

### Отладка локально

```bash
# Установите Vercel CLI
npm i -g vercel

# Запустите локально
vercel dev
```

### Обновление кода

```bash
git add .
git commit -m "Update"
git push

# Vercel автоматически задеплоит изменения
```

## Лицензия

MIT

## Контакты

GitHub: [@ArtemFilin1990](https://github.com/ArtemFilin1990)